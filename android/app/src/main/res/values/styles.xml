<resources>
    <style name="AppTheme" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">#000000</item>
        <item name="android:colorAccent">#A855F7</item>
        <item name="android:navigationBarColor">#000000</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <!-- Startup gets a real drawable immediately, before StartupActivity.onCreate finishes. -->
    <style name="StartupTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/loading_cow</item>
        <item name="android:windowDisablePreview">false</item>
    </style>
</resources>
